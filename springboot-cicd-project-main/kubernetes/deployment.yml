apiVersion: apps/v1
kind: Deployment

metadata:
  name: spring-boot-app
  namespace: devops

spec:
  replicas: 1

  selector:
    matchLabels:
      app: spring-boot-app

  template:

    metadata:
      labels:
        app: spring-boot-app

    spec:

      containers:

      - name: spring-boot-app

        image: kusuma3121/springboot:6

        imagePullPolicy: Always

        ports:
        - containerPort: 8081

        envFrom:
        - configMapRef:
            name: springboot-config

        resources:

          requests:
            cpu: "50m"
            memory: "64Mi"

          limits:
            cpu: "300m"
            memory: "256Mi"

        livenessProbe:

          httpGet:
            path: /employees/health
            port: 8081

          initialDelaySeconds: 20
          periodSeconds: 10

        readinessProbe:

          httpGet:
            path: /employees/health
            port: 8081

          initialDelaySeconds: 10
          periodSeconds: 5
